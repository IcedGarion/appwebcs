@{
    ViewData["Title"] = "Upo e-Commerce - PRIVATE HOME";
    var ruolo = HttpContextAccessor.HttpContext.Session.GetString("Ruolo");
}

<div class="container">
    <div class="main">
        <h1>Area Amministrazione</h1>
        <!-- DA TOGLIERE DOPO AUTH -->
        @if (ruolo == null)
        {
            <h1>Non sei loggato!</h1>
        }
        else if (ruolo.Equals("user"))
        {
            <h1>Area riservata agli amministratori!</h1>
        }
        else
        {
            <div id="greybar"><br /><br /></div>
            <div class="ph">
                <div id="links">
                    @Html.ActionLink("Gestione Utenti", "List", "Utente", null, new { @class = "linkprod" })
                    <br /><br /><br />
                    @Html.ActionLink("Gestione Prodotti", "List", "Prodotto", null, new { @class = "linkprod" })
                    <br /><br /><br />
                    @Html.ActionLink("Gestione Ordini", "List", "Ordine", null, new { @class = "linkprod" })
                </div>
            </div>
        }
    </div>
    <div class="footer">
        <h6>footer</h6>
    </div>
</div>
